{
  "name": "legal-signing-system",
  "version": "1.0.0",
  "description": "Legal Document Signing System with Multi-Party Workflows and Notary Support",
  "main": "backend/dist/server.js",
  "scripts": {
    "dev:server": "cd backend && npm run dev",
    "dev:client": "cd frontend && npm start",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "build:server": "cd backend && npm run build",
    "build:client": "cd frontend && npm run build",
    "build": "npm run build:server && npm run build:client",
    "start": "cd backend && npm start",
    "seed": "cd backend && npm run seed",
    "prisma:studio": "cd backend && npx prisma studio",
    "prisma:migrate": "cd backend && npx prisma migrate dev",
    "test": "npm run test:server && npm run test:client",
    "test:server": "cd backend && npm test",
    "test:client": "cd frontend && npm test",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": [
    "legal",
    "document",
    "signing",
    "esignature",
    "notary",
    "prenup",
    "contract"
  ],
  "author": "Legal Signing System Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
